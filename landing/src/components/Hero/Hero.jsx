import React from 'react';
import HeroClient from './HeroClient';

const Hero = () => {
  return (
    <section className="w-full pt-16 pb-20">
      <HeroClient />
    </section>
  );
};

export default Hero;